# Ownable Std (Practical)

`ownable-std` provides shared message types, schema helpers, and macros used by Ownables contracts.

Use it to avoid drift between wallet, SDK, and contract message formats.

Practical rule: keep contract message types explicit and versioned. Do not rely on implicit JSON shape assumptions.

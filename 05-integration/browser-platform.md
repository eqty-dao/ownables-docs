# Browser Platform

`@ownables/platform-browser` provides browser-native utilities:

- `IDBService`
- `LocalStorageService`
- `SessionStorageService`
- `PackageService`
- `calculateCid`

Use IndexedDB for canonical local state. Use local or session storage only for non-critical cached data.

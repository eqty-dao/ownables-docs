# Node Platform

`@ownables/platform-node` supports server-side integration.

Key components:

- `BucketStateStore`
- `NodeSandboxOwnableRPC`
- `NodePackageAssetIO`

Use this layer when you need backend verification, ingestion workers, or controlled execution environments.

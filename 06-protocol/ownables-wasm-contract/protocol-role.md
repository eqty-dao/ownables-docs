# Protocol Role

The Ownables WASM contract acts as an admission and append engine.

It defines and enforces protocol boundaries for accepted events and queryable state surfaces.

It is not a full external chain verifier.

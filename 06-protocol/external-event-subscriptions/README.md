# External Event Subscriptions

Subscriptions define which external events are required or admissible for an Ownable.

They are protocol configuration, not UI hints.

The subscription set is fixed at `init` to guarantee deterministic replay and avoid timeline ambiguity.

# Security

Security in Ownables protocol is split across components.

Contracts enforce admissibility and local invariants.
Wallet or verifier enforces full reconciliation and detects invalid operational states.
